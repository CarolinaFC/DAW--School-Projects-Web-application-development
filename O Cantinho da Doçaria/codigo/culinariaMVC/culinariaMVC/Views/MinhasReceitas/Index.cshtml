@model IEnumerable<culinariaMVC.Models.MinhasReceita>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_receitaAPI)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_leitorNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_receitaAPI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_leitorNavigation.Id_leitor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_minhaReceita">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_receitaAPI">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_receitaAPI">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
